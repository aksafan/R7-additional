<!DOCTYPE html>
<html>
<head>
  <title>R7Additional</title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  <%= javascript_importmap_tags %>
</head>

<body>
<div class="container-fluid">
  <div class="mt-4 p-5 bg-primary text-white rounded banner-background">
    <h1>Forum</h1>
    <p>With customers and orders. Dont ask why this is a forum xD</p>
  </div>
  <% if flash.alert.present? %>
    <div class="alert alert-danger">
      <%= flash.alert %>
    </div>
  <% end %>
  <% if flash.notice.present? %>
    <div class="alert alert-success">
      <%= flash.notice %>
    </div>
  <% end %>

  <nav class="navbar navbar-expand-sm bg-light">

    <div class="container-fluid">
      <ul class="navbar-nav">
        <li class="nav-item">
          <%= link_to "Customers", customers_path, class: "btn dropdown-toggle" %>
        </li>
        <li class="nav-item">
          <%= link_to "Orders", orders_path, class: "btn dropdown-toggle" %>
        </li>
      </ul>
    </div>

  </nav>
  <%= yield %>
</div>
</body>
</html>
